Designer Trial is a maven application with a built-in jetty server, run the following command to start the application;

mvn jetty:run

Then, navigate to url below to access the Trial

http://localhost:8080/designer-trial